const backend_url = "http://127.0.0.1:8000/";

const register_endpoint = `${backend_url}register`;
const login_endpoint = `${backend_url}login`;

export { backend_url, register_endpoint, login_endpoint };
